program P_pelanggan;

uses
  Forms,
  pelanggan in 'pelanggan.pas' {Form1},
  EntryPelanggan in 'EntryPelanggan.pas' {Form2};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TForm2, Form2);
  Application.Run;
end.
